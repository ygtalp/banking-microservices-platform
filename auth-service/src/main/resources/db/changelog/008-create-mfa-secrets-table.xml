<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="008-create-mfa-secrets-table" author="claude">
        <createTable tableName="mfa_secrets">
            <!-- Primary Key -->
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true"/>
            </column>

            <!-- User Reference (One-to-One) -->
            <column name="user_id" type="BIGINT">
                <constraints nullable="false" unique="true"
                             foreignKeyName="fk_mfa_secrets_user_id"
                             references="users(id)"
                             deleteCascade="true"/>
            </column>

            <!-- TOTP Secret -->
            <column name="totp_secret" type="VARCHAR(64)">
                <constraints nullable="true"/>
            </column>

            <!-- SMS OTP Secret -->
            <column name="sms_secret" type="VARCHAR(64)">
                <constraints nullable="true"/>
            </column>

            <!-- Email OTP Secret -->
            <column name="email_secret" type="VARCHAR(64)">
                <constraints nullable="true"/>
            </column>

            <!-- Backup Codes -->
            <column name="backup_codes" type="TEXT">
                <constraints nullable="true"/>
            </column>

            <column name="backup_codes_used" type="TEXT">
                <constraints nullable="true"/>
            </column>

            <!-- MFA Status -->
            <column name="enabled" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="preferred_method" type="VARCHAR(20)">
                <constraints nullable="true"/>
            </column>

            <!-- Verification Status -->
            <column name="totp_verified" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="sms_verified" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="email_verified" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <!-- Last Used -->
            <column name="last_used_at" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>

            <column name="last_used_method" type="VARCHAR(20)">
                <constraints nullable="true"/>
            </column>

            <!-- Audit Fields -->
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Indexes -->
        <createIndex tableName="mfa_secrets" indexName="idx_mfa_user_id">
            <column name="user_id"/>
        </createIndex>

        <createIndex tableName="mfa_secrets" indexName="idx_mfa_enabled">
            <column name="enabled"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
