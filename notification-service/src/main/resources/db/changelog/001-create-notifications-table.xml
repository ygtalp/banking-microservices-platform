<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="001-create-notifications-table" author="notification-service">
        <createTable tableName="notifications">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <!-- Notification Identification -->
            <column name="notification_id" type="VARCHAR(50)">
                <constraints nullable="false" unique="true"/>
            </column>

            <!-- User & Recipient Information -->
            <column name="user_id" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="recipient" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <!-- Notification Details -->
            <column name="channel" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="priority" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="template_code" type="VARCHAR(50)"/>
            <column name="subject" type="VARCHAR(500)"/>
            <column name="content" type="TEXT">
                <constraints nullable="false"/>
            </column>

            <!-- Status Tracking -->
            <column name="status" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="error_message" type="TEXT"/>
            <column name="retry_count" type="INT" defaultValue="0"/>
            <column name="max_retries" type="INT" defaultValue="3"/>

            <!-- Timing Information -->
            <column name="scheduled_at" type="TIMESTAMP"/>
            <column name="sent_at" type="TIMESTAMP"/>
            <column name="delivered_at" type="TIMESTAMP"/>
            <column name="read_at" type="TIMESTAMP"/>

            <!-- External Integration -->
            <column name="external_id" type="VARCHAR(255)"/>

            <!-- Audit Fields -->
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP"/>
        </createTable>

        <!-- Indexes for Performance -->
        <createIndex tableName="notifications" indexName="idx_notification_user_id">
            <column name="user_id"/>
        </createIndex>

        <createIndex tableName="notifications" indexName="idx_notification_status">
            <column name="status"/>
        </createIndex>

        <createIndex tableName="notifications" indexName="idx_notification_channel">
            <column name="channel"/>
        </createIndex>

        <createIndex tableName="notifications" indexName="idx_notification_created_at">
            <column name="created_at"/>
        </createIndex>
    </changeSet>

    <!-- Create notification_parameters table for ElementCollection -->
    <changeSet id="001-create-notification-parameters-table" author="notification-service">
        <createTable tableName="notification_parameters">
            <column name="notification_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_notification_parameters_notification"
                             references="notifications(id)" deleteCascade="true"/>
            </column>
            <column name="param_key" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="param_value" type="VARCHAR(1000)"/>
        </createTable>

        <createIndex tableName="notification_parameters" indexName="idx_notification_params_notification_id">
            <column name="notification_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
